## Instrumec Proof of delivery automation

### Getting started

1. Install dependencies

```bash

python3 -m pipenv install
```

2. Run application

```bash
python3 -m pipenv run uvicorn app:app --reload
```
